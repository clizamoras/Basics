class Solution:
  def sort(self,arr):
    n=len(arr)
    for i in range(n):
        temp=arr[i]
        j=i-1
        while j>=0 and arr[j]>temp:
            arr[j+1]=arr[j]
            j-=1
        arr[j+1]=temp
    return arr
arr=[1,4,3,2]
obj=Solution()
print(obj.sort(arr))
          

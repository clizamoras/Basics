class Node:
    def __init__(self,data):
        self.data=data
        self.next=None
class Solution:
    def insertion(self,head,val):
        new=Node(val)
        new.next=head
        head=new
        return head
head=Node(10)
head.next=Node(20)
head.next.next=Node(30)
obj=Solution()
head=obj.insertion(head,5)
temp=head
while(temp):
    print(temp.data)
    temp=temp.next

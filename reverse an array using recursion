class Solution:
    def reverse(self, arr, s, e):
        if s >= e:
            return
        
        arr[s], arr[e] = arr[e], arr[s]
        self.reverse(arr, s+1, e-1)


arr = [1, 2, 3, 4, 5]
obj = Solution()

print(obj.reverse(arr, 0, len(arr)-1))
